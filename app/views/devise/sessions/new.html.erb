<div class="container justify-content-center col-sm-4">
  <div class="form-container">
    <h2>Entrar na sua conta</h2>

    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
        <%= f.input :email,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "e-mail" } %>
        <%= f.input :password, label: "Senha",
                    required: false,
                    input_html: { autocomplete: "senha atual" } %>
        <%= f.input :remember_me, as: :boolean, label: "Me lembre" if devise_mapping.rememberable? %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "Entre na sua conta", class: "btn btn-ghost" %>
      </div>
    <% end %>
    <%= render "devise/shared/links" %>
  </div>
</div>


